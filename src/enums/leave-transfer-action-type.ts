export enum LeaveTransferActionType {
  RenewLeaveBalance = 1, // إضافة رصيد (Add Balance)
  TransferBalance = 2, // ترحيل للأمام (Forward Transfer)
  ReverseTransferBalance = 3, // ترحيل عكسي (Backward Transfer)
}
